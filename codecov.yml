coverage:
  status:
    project:
      default:
        target: auto      # must not drop below current project coverage
        threshold: 1%     # allow up to 1% drop before failing
    patch:
      default:
        target: 90%       # new/changed lines must be at least 90% covered
        threshold: 0%
