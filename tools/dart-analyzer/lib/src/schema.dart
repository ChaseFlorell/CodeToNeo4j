/// Neo4j graph schema constants for the `src__` namespace.
class GraphSchema {
  GraphSchema._();

  static const String dependsOn = 'src__DEPENDS_ON';
  static const String contains = 'src__CONTAINS';
  static const String invokes = 'src__INVOKES';
}